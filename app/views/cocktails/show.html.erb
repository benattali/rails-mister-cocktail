<h1><%= @cocktail.name %></h1>

<% @cocktail.doses.each do |dose| %>
  <h4>
    <%= dose.description %>
    <%= dose.ingredient.name %>
    <%= link_to "x", dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } %>
  </h4>
<% end %>
<p> <%= link_to "Add a dose", new_cocktail_dose_path(@cocktail) %> </p>
<p> <%= link_to "Back", cocktails_path %> </p>
